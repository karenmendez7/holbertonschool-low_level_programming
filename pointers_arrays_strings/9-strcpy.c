#include "main.h"
/**
 * _strcpy - copies string pointed
 * @dest: pointer dest
 * @src: pointer src
 * Return: dest
 */
char *_strcpy(char *dest, char *src)
{
int i;
for (i = 0; i >= 0; i++)
{
src = dest;
}
return (dest);
}
